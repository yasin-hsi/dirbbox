import 'package:flutter/material.dart';

class StorageDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Storage Details Page')),
      body: const Center(child: Text('STORAGE DETAILS')),
    );
  }
}
